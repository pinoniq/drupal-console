name: '{{ module }}'
type: {{ type }}
description: '{{ description }}'
core_version_requirement: {{ core }}
package: '{{ package }}'
{% if dependencies %}
dependencies:
{% for dependency in dependencies %}
  - {{ dependency }}
{% endfor %}
{% endif %}
